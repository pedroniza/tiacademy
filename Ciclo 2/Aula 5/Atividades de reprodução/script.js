	import {msgLog} from './modulos/modulos.js';
	msgLog("Olá");

	window.onload=function(){
	//msgLog("Olá");

	/*
	var data = new Date();
	//var dta = data.setDate( data.getDate()+3 );
	//console.log(dta);

	semana =["dom", "seg", "ter", "qua", "qui", "sex", "sab"]

	console.log(semana[data.getDay()]);

	var dta= data.toLocaleString('pt-BR', {year:'numeric', month:'2-digit',	day:'2-digit'});
	console.log(dta);
	

	var n='x';
	try{
		n=Number(n);
		if(isNaN(n)) throw "Este valor não é um número";

	} catch(erro){
		console.log(`Erro: ${erro}`) //se não retornar nada, passou no teste
	} finally{
		console.log("Terminando");
	}
	*/
	}